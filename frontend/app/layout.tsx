import type { Metadata } from 'next';
import { Inter, Space_Grotesk } from 'next/font/google';
import './globals.css';

const inter = Inter({
  subsets: ['latin'],
  variable: '--font-inter',
  display: 'swap',
});

const spaceGrotesk = Space_Grotesk({
  subsets: ['latin'],
  variable: '--font-space-grotesk',
  display: 'swap',
});

export const metadata: Metadata = {
  title: 'Lynsea — Decision Outcome Simulator',
  description:
    'Simulate probabilistic parallel futures for hard life decisions. Map out consequences with AI-driven personas to navigate uncertainty with clarity.',
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body
        className={`${inter.variable} ${spaceGrotesk.variable} font-body bg-canvas text-text-primary min-h-screen antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
